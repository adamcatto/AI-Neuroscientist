The plans are fine. Still, the autonomous coding is awful. It gets stuck with third-party libraries especially.

1. we will definitely need to do RAG with third-party libraries' documentation. The agent gets stuck on the most trivial of tasks which could be overcome simply by letting it search through documentation.

2. The complexity is growing such that using a framework dedicated to LLM-based multi-agent systems. Probably will use LlamaIndex.

## Next Steps:

1. LlamaIndex refactor

2. RAG with third-party library source code / documentation (e.g. scanpy, anndata)